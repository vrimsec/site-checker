services:
  site-checker:
    image: site-checker:local
    build:
      context: .
    # Save results inside this repo folder (portable)
    volumes:
      - ./runs:/runs
    environment:
      - RUNS_DIR=/runs
    entrypoint: ["python3", "/app/site_check.py"]
    stdin_open: true
    tty: true
